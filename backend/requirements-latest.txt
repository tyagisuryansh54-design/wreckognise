# Unpinned floors -- use this on Python 3.12 / 3.13 / 3.14, where the exact
# pins in requirements.txt have no wheels. This is the combination the project
# was actually smoke-tested against (FastAPI 0.141, NumPy 2.5, OpenCV 5.0,
# Pydantic 2.13 on CPython 3.14).
#
#     pip install -r requirements-latest.txt

fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.20
pydantic>=2.10
pydantic-settings>=2.7

# Sonar ingestion. Optional: without it, .xtf falls back to the modelled swath.
# pyxtf>=1.4

# Imaging / preprocessing
opencv-python-headless>=4.10
numpy>=1.26
Pillow>=11.0

# AI detection engine. onnxruntime serves the trained YOLOv8 weights in
# backend/models/. Torch is deliberately NOT a dependency: the deployed
# instance has 512 MB RAM and onnxruntime needs a fraction of that.
onnxruntime>=1.20

# Test client
httpx>=0.27
